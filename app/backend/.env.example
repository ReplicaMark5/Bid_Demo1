# Environment variables for Supply Chain Optimizer Backend

# GitHub AI Integration (Optional)
# Get your token from: https://github.com/settings/tokens
GITHUB_TOKEN=your_github_token_here

# Server Configuration
HOST=0.0.0.0
PORT=8000
RELOAD=true

# CORS Configuration
CORS_ORIGINS=["http://localhost:3000", "http://127.0.0.1:3000"]

# File Upload Configuration
MAX_FILE_SIZE=50000000  # 50MB in bytes
UPLOAD_FOLDER=uploads

# Optimization Configuration
DEFAULT_EPSILON_POINTS=21
DEFAULT_CONSTRAINT_TYPE=cost
DEFAULT_RANDOM_SEED=42

# Database Configuration (if needed in future)
# DATABASE_URL=sqlite:///./optimizer.db